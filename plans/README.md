# Implementation Plans

Generated by the improve skill on 2026-08-12, extended 2026-08-14. Execute in the order below
unless dependencies say otherwise. Each executor must read the selected plan fully, honor its STOP
conditions, and update the status row when done.

## Execution order and status

| Plan | Title                                                   | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Make player reload failure an atomic unload transition  | P1       | M      | —          | DONE   |
| 002  | Give media a core play-count domain and one native path | P1       | M      | —          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- Plan 001 is self-contained and already landed.
- Plan 002 is self-contained. It follows the slider definition-parser pattern but does not depend
  on slider files remaining unchanged.

## Findings considered and rejected

- Retaining the previous session as a rollback after displaying the load-error view was rejected.
  It would preserve the current contradiction between visible content and callable state. A host
  that wants to retain the previous item can avoid issuing the replacement load until its input is
  ready.
- Clearing the existing item at the start of every `loadUrl` was rejected. Keeping the last
  committed item during the pending fetch avoids unnecessary blanking; the generation guard and
  terminal transition define the commit boundary.
- Moving media pause-delay/duration timers into core was rejected. Those attributes are shared
  vocabulary presentation; the play-session machine only needs the player's `pauseState` flag.
- Routing the media writer through `QtiInteraction` / `QtiMediaDefinition` was rejected. Writer
  keeps its own builder input, same as slider.
- Building custom media chrome for `data-qti-media-player-controls` tokens other than `none` was
  rejected. Native controls remain the operable widget.
